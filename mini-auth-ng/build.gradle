apply plugin: 'com.moowork.node'
apply plugin: 'base'

clean {
    delete 'dist'
}

task npmRunBuild(type: NpmTask) {
    dependsOn npmInstall
    npmCommand = ['run', 'build']
    args = ['--', '--prod']
}
